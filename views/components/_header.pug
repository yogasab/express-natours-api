header.header
  nav.nav.nav--tours
    a.nav__el(href="/") All Tours
  nav.nav.nav--user
    if user
      a.nav__el.nav__el--logout Logout
      a.nav__el
        //- img.nav__user-img(src=`https://i.pravatar.cc/300` alt=`${user.name}`)
        span= user.name.split(" ")[0]
    else 
      a.nav__el.nav__el--cta(href="/login") Login
      a.nav__el.nav__el--cta(href="/register") Register